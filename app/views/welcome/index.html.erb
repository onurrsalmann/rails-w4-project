<h1>BooksShare</h1>

<h2>Books</h2>
<% if @books.any? %>
  <table>
    <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th>Username</th>
    </tr>
    </thead>
    <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.username %></td>
        <% if current_user %>
          <td><%= link_to 'Show', book %></td>
        <% end %>
      </tr>
    <% end %>
    </tbody>
  </table>
<% else %>
  <h3>Henüz hiç kitap eklenmemiş.</h3>
<% end %>